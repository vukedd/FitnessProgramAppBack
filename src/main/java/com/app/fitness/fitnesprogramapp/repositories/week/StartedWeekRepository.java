package com.app.fitness.fitnesprogramapp.repositories.week;

import com.app.fitness.fitnesprogramapp.models.week.StartedWeek;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StartedWeekRepository extends JpaRepository<StartedWeek, Long> {
}
